{
  "properties": {
    "name": {
      "type": "string",
      "maxLength": 64,
      "minLength": 1,
      "pattern": "^[a-zA-Z0-9_-]+$"
    },
    "description": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "parameters": {
      "anyOf": [
        {
          "$ref": "./EmptyModelParam.json"
        },
        {
          "type": "null"
        }
      ]
    },
    "strict": {
      "type": "boolean"
    },
    "type": {
      "type": "string",
      "enum": [
        "function"
      ],
      "default": "function",
      "x-stainless-const": true
    },
    "cache_control": {
      "$ref": "./CacheControlParam.json",
      "description": "Optional cache control hint. When present on the last tool, indicates that the tools block should be cached."
    }
  },
  "type": "object",
  "required": [
    "name",
    "type"
  ],
  "x-openai-class-name": "responses_common.api.params.tool_param.FunctionTool"
}
