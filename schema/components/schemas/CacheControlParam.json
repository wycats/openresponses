{
  "properties": {
    "type": {
      "type": "string",
      "enum": ["ephemeral"],
      "description": "The type of cache control. Currently only `ephemeral` is supported.",
      "x-stainless-const": true
    },
    "ttl": {
      "type": "string",
      "enum": ["5m", "1h"],
      "description": "Optional time-to-live hint for the cache. Providers may ignore this if unsupported."
    }
  },
  "type": "object",
  "required": ["type"],
  "title": "Cache control",
  "description": "A cache control hint indicating that content up to and including this part should be cached. This enables prompt caching on providers that support it (e.g., Anthropic, Google Gemini).",
  "x-openai-class-name": "responses_common.api.params.cache_control_param.CacheControlParam"
}
