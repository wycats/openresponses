{
  "properties": {
    "cached_tokens": {
      "type": "integer",
      "description": "The number of input tokens that were served from cache. Providers MUST report this field when cache hints are provided and caching is supported, even if the value is 0."
    },
    "cache_write_tokens": {
      "type": "integer",
      "description": "The number of input tokens written to cache. Reported when new content is added to the cache. Subsequent requests with the same content will report cached_tokens instead."
    }
  },
  "type": "object",
  "required": [
    "cached_tokens"
  ],
  "title": "Input tokens details",
  "description": "A breakdown of input token usage including cache hit/miss information.",
  "x-openai-class-name": "responsesapi.api.resources.response_resource.InputTokensDetails"
}
