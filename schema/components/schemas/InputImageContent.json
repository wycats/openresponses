{
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "input_image"
      ],
      "description": "The type of the input item. Always `input_image`.",
      "default": "input_image",
      "x-stainless-const": true
    },
    "image_url": {
      "anyOf": [
        {
          "type": "string",
          "description": "The URL of the image to be sent to the model. A fully qualified URL or base64 encoded image in a data URL."
        },
        {
          "type": "null"
        }
      ]
    },
    "file_id": {
      "anyOf": [
        {
          "type": "string",
          "description": "The ID of the file to be sent to the model."
        },
        {
          "type": "null"
        }
      ]
    },
    "detail": {
      "$ref": "./ImageDetail.json",
      "description": "The detail level of the image to be sent to the model. One of `high`, `low`, or `auto`. Defaults to `auto`."
    },
    "cache_control": {
      "$ref": "./CacheControlParam.json",
      "description": "Optional cache control hint. When present, indicates that content up to and including this part should be cached."
    }
  },
  "type": "object",
  "required": [
    "type",
    "image_url",
    "file_id",
    "detail"
  ],
  "title": "Input image",
  "description": "An image input to the model. Learn about [image inputs](/docs/guides/vision).",
  "x-openai-class-name": "responses_common.api.resources.content_resource.InputImageContent"
}
